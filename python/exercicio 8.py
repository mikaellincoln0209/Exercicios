op = str(input("Digite S para sair: "))

if(op == "s" or op == "S"):
	print("Algoritmo Encerrado")
else:
	while(op != "s" and op != "S"):
		op = str(input("Digite S para sair: "))
	print ("algoritmo encerrado!")
